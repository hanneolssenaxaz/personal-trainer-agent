[project]
name = "personal-trainer-agent"
version = "0.1.0"
description = "Local PoC: personal training agent using Google ADK + Gemini + SQLite memory"
requires-python = ">=3.11"
dependencies = [
  "google-adk==1.31.1",
  "google-genai==1.73.1",
  "python-dotenv==1.2.2",
]

[tool.uv]
dev-dependencies = []

